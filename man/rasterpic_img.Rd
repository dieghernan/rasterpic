% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rasterpic_img.R
\encoding{UTF-8}
\name{rasterpic_img}
\alias{rasterpic_img}
\alias{rasterpic_img.sf}
\alias{rasterpic_img.sfc}
\alias{rasterpic_img.sfg}
\alias{rasterpic_img.stars}
\alias{rasterpic_img.bbox}
\alias{rasterpic_img.numeric}
\alias{rasterpic_img.SpatRaster}
\alias{rasterpic_img.SpatVector}
\alias{rasterpic_img.SpatExtent}
\title{Geotag an image as a \code{SpatRaster}}
\usage{
rasterpic_img(
  x,
  img,
  halign = 0.5,
  valign = 0.5,
  expand = 0,
  crop = FALSE,
  ...
)

\method{rasterpic_img}{sf}(
  x,
  img,
  halign = 0.5,
  valign = 0.5,
  expand = 0,
  crop = FALSE,
  ...,
  mask = FALSE,
  inverse = FALSE
)

\method{rasterpic_img}{sfc}(
  x,
  img,
  halign = 0.5,
  valign = 0.5,
  expand = 0,
  crop = FALSE,
  ...,
  mask = FALSE,
  inverse = FALSE
)

\method{rasterpic_img}{sfg}(
  x,
  img,
  halign = 0.5,
  valign = 0.5,
  expand = 0,
  crop = FALSE,
  ...,
  mask = FALSE,
  inverse = FALSE,
  crs = NULL
)

\method{rasterpic_img}{stars}(
  x,
  img,
  halign = 0.5,
  valign = 0.5,
  expand = 0,
  crop = FALSE,
  ...
)

\method{rasterpic_img}{bbox}(
  x,
  img,
  halign = 0.5,
  valign = 0.5,
  expand = 0,
  crop = FALSE,
  ...,
  crs = NULL
)

\method{rasterpic_img}{numeric}(
  x,
  img,
  halign = 0.5,
  valign = 0.5,
  expand = 0,
  crop = FALSE,
  ...,
  crs = NULL
)

\method{rasterpic_img}{SpatRaster}(
  x,
  img,
  halign = 0.5,
  valign = 0.5,
  expand = 0,
  crop = FALSE,
  ...
)

\method{rasterpic_img}{SpatVector}(
  x,
  img,
  halign = 0.5,
  valign = 0.5,
  expand = 0,
  crop = FALSE,
  ...,
  mask = FALSE,
  inverse = FALSE
)

\method{rasterpic_img}{SpatExtent}(
  x,
  img,
  halign = 0.5,
  valign = 0.5,
  expand = 0,
  crop = FALSE,
  ...,
  crs = NULL
)
}
\arguments{
\item{x}{An \R object. See \strong{S3 methods} for supported classes.}

\item{img}{An image to geotag. It can be a local file or a URL, for example
\code{"https://i.imgur.com/6yHmlwT.jpeg"}. Accepted file extensions are \code{png},
\code{jpg}, \code{jpeg}, \code{tif} and \code{tiff}.}

\item{halign}{A number between \code{0} and \code{1} giving the horizontal alignment of
\code{img} relative to \code{x}. \code{0} aligns \code{img} with the left edge of \code{x}, \code{1}
aligns it with the right edge and \code{0.5} centers it horizontally.}

\item{valign}{A number between \code{0} and \code{1} giving the vertical alignment of
\code{img} relative to \code{x}. \code{0} aligns \code{img} with the bottom edge of \code{x}, \code{1}
aligns it with the top edge and \code{0.5} centers it vertically.}

\item{expand}{An expansion factor of the bounding box of \code{x}. \code{0} means that
no expansion is added, \code{1} means that the bounding box is expanded to
double the original size. See \strong{Details}.}

\item{crop}{Logical. Should the raster be cropped to the (expanded) bounding
box of \code{x}? See \strong{Details}.}

\item{...}{Further arguments passed to methods.}

\item{mask}{Logical, for vector methods. Should the raster be
\link[terra:mask]{masked} to the shape of \code{x}? See \strong{Details}.}

\item{inverse}{Logical. Only used when \code{mask = TRUE}. If \code{TRUE}, areas of
the raster covered by \code{x} are masked.}

\item{crs}{Character string describing a CRS. This parameter only applies
when \code{x} is a \code{SpatExtent}, \code{sfg}, \code{bbox} or a numeric coordinate vector.
See the \strong{CRS} section.}
}
\value{
A \code{SpatRaster} object (see \code{\link[terra:rast]{terra::rast()}}) where each layer corresponds to
a color channel of \code{img}:
\itemize{
\item If \code{img} has at least 3 layers, the result records layers 1 to 3 as the
red, green and blue channels with names \code{"r"}, \code{"g"} and \code{"b"} and \code{alpha}
if applicable.
\item If \code{img} already has an RGB specification (this may be the case for
\code{tif}/\code{tiff} files), the result keeps that specification.
}

The resulting \code{SpatRaster} will have an RGB specification as explained in
\code{\link[terra:RGB]{terra::RGB()}}.
}
\description{
Geotag an image and return a \code{SpatRaster} based on coordinates from a
supported spatial input class.

\code{rasterpic_img()} is an S3 generic. See \strong{S3 methods} for supported input
classes.
}
\details{
\code{vignette("rasterpic", package = "rasterpic")} explains the effect of
parameters \code{halign}, \code{valign}, \code{expand}, \code{crop} and \code{mask} with examples.
\subsection{S3 methods}{

\CRANpkg{rasterpic} supports these spatial input classes:
\itemize{
\item \CRANpkg{sf} classes: \code{sf}, \code{sfc}, \code{sfg} and \code{bbox}.
\item \CRANpkg{terra} classes: \code{SpatRaster}, \code{SpatVector} and \code{SpatExtent}.
\item \CRANpkg{stars} class: \code{stars}.
\item A numeric coordinate vector of the form \code{c(xmin, ymin, xmax, ymax)}.
}

Other packages can provide methods for additional spatial classes.

Methods for extent-like inputs use the object extent. Methods for vector
inputs can also mask the image to the object shape.
}

\subsection{CRS}{

This function preserves the CRS of \code{x} when applicable. For optimal results,
\strong{do not use} geographic coordinates (longitude/latitude).

\code{crs} can be in WKT format, as an \code{"authority:number"} code such as
\code{"EPSG:4326"} or as a PROJ-string such as \code{"+proj=utm +zone=12"}. It can also
be retrieved with:
\itemize{
\item \code{\link[sf:st_crs]{sf::st_crs(25830)$wkt}}.
\item \code{\link[terra:crs]{terra::crs()}}.
\item \code{\link[tidyterra:pull_crs]{tidyterra::pull_crs()}}.
}

See the \strong{Value} and \strong{Notes} sections in \code{\link[terra:crs]{terra::crs()}}.
}
}
\examples{
\donttest{
library(sf)
library(terra)
library(ggplot2)
library(tidyterra)

x_path <- system.file("gpkg/UK.gpkg", package = "rasterpic")
x <- st_read(x_path, quiet = TRUE)
img <- system.file("img/vertical.png", package = "rasterpic")

# Use the default configuration.
ex1 <- rasterpic_img(x, img)

ex1

autoplot(ex1) +
  geom_sf(data = x, fill = NA, color = "white", linewidth = 0.5)

# Expand the bounding box.
ex2 <- rasterpic_img(x, img, expand = 0.5)

autoplot(ex2) +
  geom_sf(data = x, fill = NA, color = "white", linewidth = 0.5)

# Align the image to the left edge.
ex3 <- rasterpic_img(x, img, halign = 0)

autoplot(ex3) +
  geom_sf(data = x, fill = NA, color = "white", linewidth = 0.5) +
  labs(title = "Align")

# Crop to the bounding box.
ex4 <- rasterpic_img(x, img, crop = TRUE)

autoplot(ex4) +
  geom_sf(data = x, fill = NA, color = "white", linewidth = 0.5) +
  labs(title = "Crop")

# Mask to the vector shape.
ex5 <- rasterpic_img(x, img, mask = TRUE)

autoplot(ex5) +
  geom_sf(data = x, fill = NA, color = "white", linewidth = 0.5) +
  labs(title = "Mask")

# Mask outside the vector shape.
ex6 <- rasterpic_img(x, img, mask = TRUE, inverse = TRUE)

autoplot(ex6) +
  geom_sf(data = x, fill = NA, color = "white", linewidth = 0.5) +
  labs(title = "Mask Inverse")

# Combine cropping and inverse masking.
ex7 <- rasterpic_img(x, img, crop = TRUE, mask = TRUE, inverse = TRUE)

autoplot(ex7) +
  geom_sf(data = x, fill = NA, color = "white", linewidth = 0.5) +
  labs(title = "Combine")
}
}
\seealso{
\code{vignette("rasterpic", package = "rasterpic")} for examples.

From \CRANpkg{sf}:
\itemize{
\item \code{\link[sf:st_crs]{sf::st_crs()}}.
\item \code{\link[sf:st_bbox]{sf::st_bbox()}}.
\item \code{vignette("sf1", package = "sf")} to understand how \CRANpkg{sf} organizes
\R objects.
}

From \CRANpkg{stars}:
\itemize{
\item \code{\link[stars:st_as_stars]{stars::st_as_stars()}}.
}

From \CRANpkg{terra}:
\itemize{
\item \code{\link[terra:vect]{terra::vect()}}, \code{\link[terra:rast]{terra::rast()}} and \code{\link[terra:ext]{terra::ext()}}.
\item \code{\link[terra:mask]{terra::mask()}}.
\item \code{\link[terra:crs]{terra::crs()}}.
\item \code{\link[terra:RGB]{terra::RGB()}}.
}

For plotting:
\itemize{
\item \code{\link[terra:plot]{terra::plot()}} and \code{\link[terra:plotRGB]{terra::plotRGB()}}.
\item \code{\link[tidyterra:autoplot.SpatRaster]{tidyterra::autoplot.SpatRaster()}} and
\code{\link[tidyterra:geom_spatraster_rgb]{tidyterra::geom_spatraster_rgb()}} with \CRANpkg{ggplot2}.
\item \CRANpkg{tmap}, \CRANpkg{mapsf} and \CRANpkg{maptiles}.
}
}
